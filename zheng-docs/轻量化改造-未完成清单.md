# AstronRPA 后端轻量化改造 —— 未完成清单

> 生成日期：2026-07-07（最后更新：2026-07-11）
> 基于 [后端轻量化改造-InsForge规范方案](./后端轻量化改造-InsForge规范方案.md)
> 参考 [轻量化改造方案-最终版](./轻量化改造方案-最终版.md)
> 开发报告：[后端轻量化改造-开发报告.md](./后端轻量化改造-开发报告.md)（详细模块说明、验证记录、命令参考）
>
> **仓库说明**：
> - **[后端/InsForge]** — `/home/zhengxueen/github-project/InsForge/`：平台侧代码（Edge Functions 运行时、数据库迁移、Storage 配置等）
> - **[前端/astron-rpa]** — `/home/zhengxueen/github-project/astron-rpa/`：前端应用和引擎代码（修改调用方，从 Java 服务改为 InsForge SDK/API）

---

## 总体进度

| 状态 | 数量 | 说明 |
|------|------|------|
| ✅ 已完成 | Fix-1 ~ Fix-10, 页面 API 迁移, 引擎适配, 服务端执行 | 代码级改造 + 引擎适配 + 服务端部署全部完成 |
| ✅ 已完成 | Fix-11: AI 服务直连改造 | CUA / OCR / 验证码等调用由本地网关改为直连 ai-service |
| ✅ 已核实 | 前端代码改造静态验证 | 2026-07-11 通过静态代码分析核实所有前端改造已完成（见下方验证明细） |
| ⏳ 待运行时验证 | 浏览器端功能验证 | 需浏览器端实际操作验证（登录/CRUD/页面展示等） |
| ✅ 已完成 | Fix-12: robot-service 业务逻辑迁移到 Edge Functions | 前后端均已完成：3 个 Edge Functions 已部署，前端/引擎调用已迁移 |
| ✅ 已完成 | Fix-13: resource-service 文件管理迁移到 InsForge Storage | 前后端均已完成 |
| ✅ 已完成 | Fix-14: 浏览器插件 RPC / trigger 远程 WebSocket | 引擎代码改造完成 |
| ✅ 已完成 | Fix-15: `src/api/` 旧调用迁移 | 13 个非保留模块共约 128 处旧 `http.` 调用，已全部改为 `rpaApi` 或 InsForge SDK |

---

## 一、Fix-6: Edge Functions 源码编写

**优先级**: P1  
**状态**: ✅ 已完成（代码级）  
**描述**: 编写 Edge Functions 源码（notify、blacklist），使用 InsForge SDK 访问数据库

### 任务列表

- [x] 创建 `functions/notify/index.ts` — 通知发送 Edge Function
- [x] 创建 `functions/blacklist/index.ts` — 黑名单检查 Edge Function

### 涉及文件

| 文件 | 仓库 | 操作 | 说明 |
|------|------|------|------|
| `InsForge/functions/astron-rpa/notify/index.ts` | [后端/InsForge] | ✅ 已创建（源码） | 通知发送：支持 email/sms/in_app，写入 notification_logs 表 |
| `InsForge/functions/astron-rpa/blacklist/index.ts` | [后端/InsForge] | ✅ 已创建（源码） | 黑名单检查：查询 blacklist 表，支持 user_id/email/ip 维度 |
| `functions.definitions` 表（notify/blacklist 记录） | [后端/InsForge] | ✅ 已注册 | 函数代码存储在数据库，由 Deno 运行时执行 |

> 注：源码存放在 `InsForge/functions/astron-rpa/` 下，部署时注册到 `functions.definitions` 表。

### 验证方式

- [x] `notify` 函数正常处理通知请求并写入日志（✅ 已验证 — 返回 200，notification_logs 表有记录）
- [x] `blacklist` 函数正常查询黑名单状态（✅ 已验证 — 返回 200，正确区分屏蔽/正常用户）

### 服务端验证结果（2026-07-09）

```bash
# notify 函数 — 200 OK
curl -X POST "http://localhost:7130/functions/notify" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"type":"in_app","recipient":"admin@astronrpa.com","message":"test"}'
# → {"success":true,"message":"Notification sent via in_app","data":{"id":"77b844ca-...","status":"sent"}}

# blacklist 函数（被屏蔽用户）— 200 OK
curl -X POST "http://localhost:7130/functions/blacklist" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"user_id":"blocked-user"}'
# → {"blacklisted":true,"reason":"test block","checked_against":["user_id = blocked-user"]}

# blacklist 函数（正常用户）— 200 OK
curl -X POST "http://localhost:7130/functions/blacklist" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"user_id":"normal-user"}'
# → {"blacklisted":false,"reason":null,"checked_against":["user_id = normal-user"]}
```

### 修复过程

Edge Functions 部署后遇到 "Security Initialization Error"，根因为 Deno 2.9.1 中 `globalThis.Deno` 不可配置（non-configurable），`worker-template.js` 尝试用 `Object.defineProperty` 重新定义导致错误。解决方案：

1. 修改 `InsForge/functions/worker-template.js`：不再重新定义 `globalThis.Deno`，改为直接 patch `Deno.env` 方法
2. 修改 notify 函数代码：移除 `client.auth.getCurrentUser()` 调用，改用 `atob()` 从 JWT 解析 `payload.sub` 获取用户 ID
3. 创建 `notification_logs` 和 `blacklist` 表，配置 RLS 策略
4. 刷新 PostgREST schema 缓存

---

## 二、Fix-8: 清理冗余依赖

**优先级**: P2  
**状态**: ✅ 已完成（无需清理）  
**描述**: 检查并移除不再需要的导出和依赖

### 任务列表

- [x] 检查 `api-client.ts` 中是否有冗余导出 — 仅有 `insforge`, `crudApi`, `rpaApi` 三个导出，无冗余
- [x] 检查 `package.json` 中是否有 Casdoor SDK 依赖 — 未发现 `casdoor` 相关依赖
- [x] 确认无需清理

### 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `frontend/packages/shared/src/api-client.ts` | ✅ 已检查 | 无冗余导出（postgrestClient/rpaCoreClient/insforgeAuthClient 均不存在） |
| `frontend/packages/components/package.json` | ✅ 已检查 | 无 Casdoor 相关依赖 |

### 验证方式

- [ ] `pnpm build` 编译通过
- [ ] 无冗余依赖残留

---

## 三、Fix-9: 引擎客户端适配（Python SDK）

**优先级**: P1  
**状态**: ✅ 已完成（代码级）  
**描述**: 创建引擎侧 InsForge Python 客户端，将引擎各服务改为通过 InsForge API 通信

### 任务列表

- [x] 创建 `engine/shared/insforge_client.py` — InsForge Python 客户端封装
- [x] 改造 `engine/servers/scheduler/` — 用 InsForge 客户端拉取任务、更新状态
- [x] 改造 `engine/servers/executor/` — 用 InsForge Storage 下载脚本、SDK 写入执行日志
- [x] 改造 `engine/servers/trigger/` — 用 InsForge 客户端管理触发规则
- [x] 改造 `engine/servers/browser-bridge/` — WebSocket → InsForge Realtime（Socket.IO）
- [x] 改造 `engine/components/astronverse-ai/` 下 AI 调用 — `llm.py` / `agent.py` / `computer_use.py` / `custom_action_screen.py` / `core_iflytek.py` / `verifycode` 改为直连 ai-service 或 InsForge AI 网关

### 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `engine/shared/insforge_client.py` | ✅ 已创建 | 封装 httpx 调用 InsForge REST API，含认证/机器人/任务/执行/文件/实时令牌 |
| `engine/servers/scheduler/apis/connector/executor.py` | ✅ 已改造 | `report_task_log()` 使用 `insforge.create_execution()` / `update_execution()` 上报任务状态 |
| `engine/servers/executor/flow/insforge_storage.py` | ✅ 已创建 | `InsForgeStorage` 类实现 `IStorage` 接口，通过 InsForge API 获取工程/流程/参数等数据 |
| `engine/servers/trigger/server/gateway_client.py` | ✅ 已改造 | `list_trigger()` / `terminal_poll_update()` / `terminal_list_task()` 使用 `insforge.fetch_triggers()` / `fetch_terminal_tasks()` |
| `engine/servers/browser-bridge/apis/socketio_route.py` | ✅ 已创建 | Socket.IO 服务端，替代原始 WebSocket，支持与 InsForge Realtime 通信 |
| `engine/components/astronverse-ai/src/astronverse/ai/api/llm.py` | ✅ 已改造 | 优先调用 InsForge AI 网关，失败回退本地 ai-service |
| `engine/components/astronverse-ai/src/astronverse/ai/agent.py` | ✅ 已改造 | `call_astron_agent` 优先从 InsForge DB 查询 auth 信息 |
| `engine/components/astronverse-ai/src/astronverse/cua/computer_use.py` | ✅ 已改造 | CUA chat 直连 ai-service，添加详细 logger |
| `engine/components/astronverse-ai/src/astronverse/cua/custom_action_screen.py` | ✅ 已改造 | 同上 |
| `engine/components/astronverse-openapi/src/astronverse/openapi/core_iflytek.py` | ✅ 已改造 | OCR 直连 ai-service，添加详细 logger |
| `engine/components/astronverse-verifycode/src/astronverse/verifycode/__init__.py` | ✅ 已改造 | 验证码识别 URL 改为 ai-service 直连 |
| `engine/components/astronverse-verifycode/src/astronverse/verifycode/core.py` | ✅ 已改造 | 调用处改为 `VerifyCodeConfig.url()`，添加 logger |

### 附加增强

- [x] 为上述 10+ 个核心文件添加详细的 `logger.info` 打印，覆盖请求参数、响应状态、异常信息等关键路径，便于运行时排查

### 验证方式

- [ ] scheduler 通过 InsForge 客户端正常拉取待执行任务
- [ ] executor 通过 InsForge Storage 下载脚本、写入执行日志
- [ ] trigger 通过 InsForge 客户端管理触发规则
- [ ] browser-bridge 通过 Socket.IO 正常接收实时推送

---

## 四、Fix-10: 数据迁移（MySQL → PostgreSQL）

**优先级**: P1  
**状态**: ✅ 已完成（代码级 + 服务端执行）  
**描述**: 创建 RPA 业务表迁移脚本，准备 pgloader 数据迁移，在 PostgreSQL 中完成表创建

### 任务列表

- [x] 创建 `backend/src/infra/database/migrations/001_create_rpa_tables.sql` — RPA 业务表迁移脚本
- [x] 准备 pgloader 迁移配置文件 `migrate.load`
- [x] 执行迁移脚本，在 PostgreSQL 中创建 9 张 RPA 业务表（新增 `astron_agent_auth`）
- [x] 启用 RLS 行级安全策略（所有业务表 + notification_logs + blacklist）
- [x] 创建实时通知触发器（execution_change_trigger）
- [x] 创建索引（每表按 user_id/status 等关键字段建立索引）
- [ ] 使用 pgloader 执行全量数据迁移（MySQL 未运行，无需迁移）
- [ ] 验证数据完整性（MySQL 无数据，跳过）

### 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/src/infra/database/migrations/001_create_rpa_tables.sql` | ✅ 已执行 | 8 张核心业务表 + RLS + 通知触发器 + 索引 |
| `migrate.load` | ✅ 已就绪 | pgloader 配置文件，待 MySQL 就绪后使用 |

### 遇到的问题

| 问题 | 解决方式 |
|------|----------|
| `CREATE POLICY IF NOT EXISTS` 语法在 PostgreSQL 18 中不支持 | 改为 `DROP POLICY IF EXISTS` + `CREATE POLICY` |
| `execution_logs` 表缺少 `user_id` 列 | 使用 `ALTER TABLE` 添加列 |
| 创建表后 PostgREST 不识别 | 执行 `NOTIFY pgrst, 'reload schema'` 刷新缓存 |

### 验证方式

- [x] 迁移脚本成功执行，8 张表结构正确
- [x] RLS 策略生效（不同用户查询返回不同数据）
- [x] 实时通知触发器已创建

---

## 五、服务端执行结果（远程服务器 172.16.100.211，2026-07-08 ~ 2026-07-09）

> 服务器为 172.16.100.211（Linux zhengxueen-BD3），通过 SSH 远程连接执行。

### 5.1 数据库

| 任务 | 状态 | 说明 |
|------|------|------|
| 执行 `001_create_rpa_tables.sql` | ✅ 已完成 | 9 张 RPA 业务表创建成功（含 `astron_agent_auth`） |
| 配置 RLS 策略 | ✅ 已完成 | 所有业务表 + notification_logs + blacklist |
| 创建索引和触发器 | ✅ 已完成 | 按 user_id/status 等关键字段建立索引 |
| 刷新 PostgREST schema 缓存 | ✅ 已完成 | `NOTIFY pgrst, 'reload schema'` |
| MySQL → PostgreSQL 数据迁移 | ⏸ 跳过 | MySQL 未运行，无数据可迁移 |

### 5.2 Edge Functions

| 任务 | 状态 | 说明 |
|------|------|------|
| 启动 Deno 运行时（端口 7133） | ✅ 已完成 | Deno 2.9.1，`server.ts` 运行中 |
| 注册 notify/blacklist 函数 | ✅ 已完成 | 写入 `functions.definitions` 表 |
| 修复 "Security Initialization Error" | ✅ 已完成 | patch `worker-template.js` 中 `Deno` 对象定义 |
| 修复 auth getCurrentUser() 失败 | ✅ 已完成 | 改用 JWT 本地解析替代 SDK auth API |
| 创建 notification_logs 表 | ✅ 已完成 | 配置 RLS 策略 |
| 创建 blacklist 表 | ✅ 已完成 | 配置 RLS 策略 |
| 测试 notify 函数 | ✅ 已完成 | 返回 200，成功写入日志 |
| 测试 blacklist 函数 | ✅ 已完成 | 返回 200，正确区分屏蔽/正常用户 |
| 启动 PostgREST 服务（端口 5430） | ✅ 已完成 | PostgREST 14.14，重启后可用 |
| 注册 rpa-terminal/rpa-execution/rpa-crud 函数 | ✅ 已完成 | 写入 `functions.definitions` 表 |
| 测试 rpa-terminal 函数 | ✅ 已完成 | 注册和心跳均返回 200 |
| 测试 rpa-execution 函数 | ✅ 已完成 | 状态更新和创建记录均返回 200 |
| 测试 rpa-crud 函数 | ✅ 已完成 | 机器人列表/详情/流程创建/组件列表均返回 200 |
| 创建 Storage bucket（rpa-files / rpa-public） | ✅ 已完成 | 私有和公开 bucket 均已创建 |
| 测试 Storage API 上传/下载/列出 | ✅ 已完成 | 上传/下载/列出功能均正常 |

### 5.3 服务状态

| 服务 | 端口 | 状态 | 说明 |
|------|------|------|------|
| ai-service（Python FastAPI） | 8001 | ✅ 运行中 | AI 推理服务，已通过 InsForge 远程验证 JWT |
| openapi-service（Python FastAPI） | 8020 | ⏸ 未启动 | 依赖 MySQL，暂不启动 |
| robot-service（Java Spring Boot） | — | ❌ 服务端未启动 | 已由 Edge Functions 替代（rpa-terminal / rpa-execution / rpa-crud），前端和引擎需改为调用 InsForge API |
| resource-service（Java Spring Boot） | — | ❌ 服务端未启动 | 已由 InsForge Storage 替代（rpa-files / rpa-public bucket），前端和引擎需改为调用 Storage API |

### 5.4 关键命令

```bash
# 启动 PostgREST（如果未运行）
/usr/local/bin/postgrest /home/zhengxueen/github-project/InsForge/postgrest.conf &

# 启动 Deno Edge Functions 运行时
cd /home/zhengxueen/github-project/InsForge/functions
deno run --allow-net --allow-read --allow-env --allow-write \
  --unstable-worker-options server.ts &

# 注册 Edge Functions 到数据库
PGPASSWORD=xqxatcdj psql -h 127.0.0.1 -U postgres -d insforge \
  -f /home/zhengxueen/github-project/InsForge/functions/register_rpa_functions.sql

# 创建 Storage bucket
curl -X POST "http://localhost:7130/api/storage/buckets" \
  -H "Authorization: Bearer <ADMIN_TOKEN>" \
  -d '{"bucketName":"rpa-files","isPublic":false}'

# 刷新 PostgREST schema 缓存
PGPASSWORD=xqxatcdj psql -h 127.0.0.1 -U postgres -d insforge \
  -c "NOTIFY pgrst, 'reload schema';"
```

## 六、前端代码改造静态验证结果

> 2026-07-11 通过静态代码分析核实所有前端/引擎改造已完成。
> 服务端已就绪（172.16.100.211），前端已启动（`pnpm dev:web` → `http://localhost:1420/`）。

### 6.1 前端 API 层（`api-client.ts`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| `/api/robot/...` 调用 | ✅ 0 处 | 全部改为 `invokeRpaFunction('rpa-crud', ...)` 或 `invokeRpaFunction('rpa-market', ...)` |
| `/api/resource/...` 调用 | ✅ 0 处 | 全部改为 `insforge.storage.from('rpa-files').*` |
| InsForge SDK 集成 | ✅ 已迁移 | `insforge.database.from('*').select()` / `insforge.functions.invoke()` / `insforge.storage.from('*')` |
| Edge Function 调用 | ✅ 已迁移 | `rpa-crud`（30+ 个 actions）、`rpa-market`（10+ 个 actions） |

### 6.2 前端文件上传（`resource.ts`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 旧 `/api/resource/file/upload` | ✅ 0 处 | 改为 `rpaApi.resources.upload(path, file)` |
| InsForge Storage bucket | ✅ 已迁移 | `rpa-files`（私有）+ `rpa-public`（公开） |

### 6.3 认证流程

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 登录/注册 | ✅ 已迁移 | `authType === 'insforge'` 分支，调用 `signInWithPassword()` / `signUp()` |
| 用户信息获取 | ✅ 已迁移 | `getCurrentAuthUser()` 替代旧 `Auth.userInfo()` |
| 租户相关 | ✅ 已移除 | InsForge 模式下隐藏 `TenantDropdown`，移除租户过期弹窗 |
| 退出登录 | ✅ 已迁移 | `signOut()` 替代旧 `logout()` |

### 6.4 引擎层

| 组件 | 检查项 | 结果 |
|------|--------|------|
| scheduler terminal.py | 注册/心跳 | ✅ 改为 `POST /functions/rpa-terminal` |
| scheduler executor.py | 结果上报 | ✅ 改为 `POST /functions/rpa-execution` |
| enterprise.py | 文件上传/下载/共享变量 | ✅ 改为 InsForge Storage + `shared_files`/`shared_variables` 表 |
| browser.py（主组件）| 浏览器 RPC | ✅ 直连 `BROWSER_BRIDGE_URL`（默认 `:19082`） |
| trigger server.py | 远程控制 | ✅ `WebSocketManager` → `RealtimeManager`，Socket.IO 接收远程命令 |
| llm.py | AI 对话 | ✅ 优先 InsForge AI 网关，失败回退 ai-service |
| agent.py | 代理认证 | ✅ 优先 InsForge DB 查询 `astron_agent_auth` 表 |

### 6.5 数据库迁移

| 检查项 | 结果 |
|--------|------|
| RPA 业务表 | ✅ 11 张表（robots/projects/workflows/tasks/executions/execution_logs/market_components/triggers/astron_agent_auth/shared_files/shared_variables） |
| RLS 策略 | ✅ 全部启用 |
| 索引 | ✅ 全部创建 |
| 触发器 | ✅ 执行通知触发器 + 自动 `user_id` 填充触发器 |

### 6.6 404 遗留项（代码级已无需处理）

| 项 | 说明 |
|----|------|
| `trigger/gateway_client.py` 中 `GATEWAY_PORT` | 用于本地 IPC（trigger ↔ scheduler 进程间通信），非旧 OpenResty 网关 |
| `picker/utils/browser.py` 中 `browser_connector` | picker 组件自有路由，不依赖旧网关 |
| `agent.py` / `llm.py` 中 `_legacy_*` 回退路径 | 保留的向后兼容 fallback，正常情况不会触发 |

---

## 七、运行时待验证

> 以下项已通过代码静态分析确认改造完成，但需浏览器端实际操作验证功能是否正常。

| 验证项 | 状态 | 说明 |
|--------|------|------|
| 前端 dev:web 启动 | ✅ 已验证 | Vite 8.1.3 启动成功，监听 `http://localhost:1420/` |
| Vue Router 导航守卫 Bug | ✅ 已修复 | 混合 `next()` + `return` 模式导致 `Invalid navigation guard`，已改为纯 `return` 模式 |
| 用户注册/登录 | ⏳ 待运行时验证 | 代码已迁移到 InsForge Auth SDK，需浏览器端实际登录测试 |
| 刷新页面 session 恢复 | ⏳ 待运行时验证 | 需浏览器端实际验证 |
| 401 自动处理 | ⏳ 待运行时验证 | 需浏览器端实际验证 |
| CRUD 操作（机器人/流程/任务） | ⏳ 待运行时验证 | 代码已迁移到 `invokeRpaFunction('rpa-crud', ...)`，需实际页面操作验证 |
| 页面数据展示 | ⏳ 待运行时验证 | 需浏览器端实际验证 |
| 计划任务 CRUD | ⏳ 待运行时验证 | 需浏览器端实际验证 |
| 版本管理 | ⏳ 待运行时验证 | 需浏览器端实际验证 |
| 文件上传/下载 | ⏳ 待运行时验证 | 代码已迁移到 InsForge Storage SDK，需实际页面操作验证 |
| WebSocket 日志连接 | ⏳ 待运行时验证 | 需浏览器端实际验证 |

---

## 八、Fix-15: `src/api/` 旧调用迁移

> 此为新发现的待处理任务。`frontend/packages/web-app/src/api/` 下仍有大量旧 `http.` 调用未迁移到 `rpaApi` 或 InsForge SDK。

**优先级**: P1  
**状态**: ✅ 已完成（代码级 + 构建验证通过）  
**描述**: 将 `src/api/` 下的 128 处旧 `http.` 调用全部改为使用 `rpaApi` 或直接调用 InsForge SDK。

### 详细统计

| 模块 | 旧调用数 | 目标迁移方式 |
|:---|:---:|:---|
| `market.ts` | 42 | `invokeRpaFunction('rpa-market', ...)` |
| `robot.ts` | 18 | `invokeRpaFunction('rpa-crud', ...)` |
| `task.ts` | 16 | `invokeRpaFunction('rpa-crud', ...)` |
| `project.ts` | 13 | `invokeRpaFunction('rpa-crud', ...)` |
| `login/login.ts` | 10 | InsForge Auth SDK |
| `setting.ts` | 8 | `invokeRpaFunction('rpa-crud', ...)` |
| `atom.ts` | 7 | `invokeRpaFunction('rpa-crud', ...)` |
| `component.ts` | 4 | `invokeRpaFunction('rpa-crud', ...)` |
| `mail.ts` | 4 | `invokeRpaFunction('rpa-crud', ...)` |
| `plugin.ts` | 3 | `invokeRpaFunction('rpa-crud', ...)` |
| `common.ts` | 1 | `invokeRpaFunction('rpa-crud', ...)` |
| `contract.ts` | 1 | `invokeRpaFunction('rpa-crud', ...)` |
| `permission.ts` | 1 | `invokeRpaFunction('rpa-crud', ...)` |
| **合计（非保留模块）** | **128** | — |

### 保留模块（不做迁移）

| 模块 | 旧调用数 | 原因 |
|:---|:---:|:---|
| `resource.ts` | 31 | P3：资源管理，保留旧 HTTP |
| `engine.ts` | 8 | P3：引擎交互，保留旧 HTTP |
| `pick.ts` | 0 | P4：picking 服务 |
| `ws/index.ts` | 0 | P4：WebSocket |

### 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `frontend/packages/web-app/src/api/market.ts` | ✅ 已完成 | 42 处旧调用，改为 `rpaApi.market.*` |
| `frontend/packages/web-app/src/api/robot.ts` | ✅ 已完成 | 18 处旧调用，改为 `rpaApi.robots.*` |
| `frontend/packages/web-app/src/api/task.ts` | ✅ 已完成 | 16 处旧调用，改为 `rpaApi.tasks.*` |
| `frontend/packages/web-app/src/api/project.ts` | ✅ 已完成 | 13 处旧调用，改为 `rpaApi.projects.*` |
| `frontend/packages/web-app/src/api/login/login.ts` | ✅ 已删除 | 10 处旧调用，已由 InsForge Auth SDK 替代（文件已删除） |
| `frontend/packages/web-app/src/api/setting.ts` | ✅ 已完成 | 8 处旧调用，改为 `rpaApi.http.*` |
| `frontend/packages/web-app/src/api/atom.ts` | ✅ 已完成 | 7 处旧调用，改为 `rpaApi.atoms.*` + `crudApi.*` |
| `frontend/packages/web-app/src/api/component.ts` | ✅ 已完成 | 4 处旧调用，改为 `rpaApi.component.*` |
| `frontend/packages/web-app/src/api/mail.ts` | ✅ 已完成 | 4 处旧调用，改为 `rpaApi.mail.*` |
| `frontend/packages/web-app/src/api/plugin.ts` | ✅ 已完成 | 3 处旧调用，改为 `rpaApi.http.*` |
| `frontend/packages/web-app/src/api/common.ts` | ✅ 已完成 | 1 处旧调用（反馈提交），改为 `crudApi.feedback.*` |
| `frontend/packages/web-app/src/api/contract.ts` | ✅ 已完成 | 1 处旧调用，改为 `rpaApi.http.*` |
| `frontend/packages/web-app/src/api/permission.ts` | ✅ 已完成 | 1 处旧调用（权限检查），改为 `rpaApi.http.*` |

### 任务列表

- [x] `market.ts` — 42 处旧调用迁移到 `rpaApi.market.*`
- [x] `robot.ts` — 18 处旧调用迁移到 `rpaApi.robots.*`
- [x] `task.ts` — 16 处旧调用迁移到 `rpaApi.tasks.*`
- [x] `project.ts` — 13 处旧调用迁移到 `rpaApi.projects.*`
- [x] `login/login.ts` — 10 处旧调用，文件已删除（已由 InsForge Auth SDK 替代）
- [x] `setting.ts` — 8 处旧调用迁移到 `rpaApi.http.*`
- [x] `atom.ts` — 7 处旧调用迁移到 `rpaApi.atoms.*` + `crudApi.*`
- [x] `component.ts` — 4 处旧调用迁移到 `rpaApi.component.*`
- [x] `mail.ts` — 4 处旧调用迁移到 `rpaApi.mail.*`
- [x] `plugin.ts` — 3 处旧调用迁移到 `rpaApi.http.*`
- [x] `common.ts` / `contract.ts` / `permission.ts` — 各 1 处迁移

### 验证方式

- [x] 迁移后 `pnpm build:web` 编译通过（exit code 0），无 TypeScript 错误
- [ ] 对应页面功能正常（市场/机器人/任务/项目/设置/组件/邮件等）— 待运行时验证
- [ ] 登录流程正常 — 待运行时验证

---

## 九、Fix-11: AI 服务直连改造

**优先级**: P1  
**状态**: ✅ 已完成（代码级）  
**描述**: 将引擎中仍通过本地网关 `127.0.0.1:13159` 调用的 AI 相关接口改为直连 ai-service。

### 任务列表

- [x] 改造 `engine/components/astronverse-cua/src/astronverse/cua/computer_use.py`
- [x] 改造 `engine/components/astronverse-cua/src/astronverse/cua/custom_action_screen.py`
- [x] 改造 `engine/components/astronverse-openapi/src/astronverse/openapi/core_iflytek.py`
- [x] 改造 `engine/components/astronverse-verifycode/src/astronverse/verifycode/__init__.py`
- [x] 改造 `engine/components/astronverse-verifycode/src/astronverse/verifycode/core.py`

### 改造方式

- 新增 `_ai_service_url()`，从环境变量 `AI_SERVICE_URL` 读取，默认 `http://172.16.100.211:8001`
- 移除对 `atomicMg.cfg().get("GATEWAY_PORT")` / `13159` 的依赖
- 在关键路径添加 `logger.info` 打印请求 URL、状态码、异常信息

### 涉及端点映射

| 旧路径（本地网关） | 新路径（ai-service 直连） |
|-------------------|--------------------------|
| `/api/rpa-ai-service/cua/chat` | `/cua/chat` |
| `/api/rpa-ai-service/ocr/general` | `/ocr/general` |
| `/api/rpa-ai-service/jfbym/customApi` | `/jfbym/customApi` |

---

## 十、Fix-12: robot-service 业务逻辑迁移到 Edge Functions

**优先级**: P2  
**状态**: ✅ 已完成  
**描述**: robot-service 的业务逻辑已通过 Edge Functions 替代，包括机器人注册/心跳、执行状态上报、CRUD 操作等。前端/引擎调用已全部迁移。

### 改造范围

| 侧 | 仓库 | 说明 |
|----|------|------|
| **[后端/InsForge]** | `InsForge/` | 已编写 5 个 Edge Functions 替代 robot-service 业务逻辑 ✅ |
| **[前端/astron-rpa]** | `astron-rpa/frontend/` | 待修改前端 API 调用，从 robot-service 改为 InsForge SDK |
| **[前端/astron-rpa]** | `astron-rpa/engine/` | 待修改引擎 terminal/executor 调用，改为 `insforge_client.py` 操作 DB |

### 涉及文件

| 文件 | 仓库 | 当前状态 | 改造目标 |
|------|------|----------|----------|
| `InsForge/functions/astron-rpa/rpa-terminal/index.ts` | [后端/InsForge] | ✅ 已创建并注册 | 终端注册/心跳，替代 `POST /api/robot/terminal/register` + `beat` |
| `InsForge/functions/astron-rpa/rpa-execution/index.ts` | [后端/InsForge] | ✅ 已创建并注册 | 执行状态上报，替代 `POST /api/robot/task-execute/status` + `save-result` |
| `InsForge/functions/astron-rpa/rpa-crud/index.ts` | [后端/InsForge] | ✅ 已创建并注册 | 机器人 CRUD、流程管理、组件管理、版本管理、全局变量管理 |
| `InsForge/functions/register_rpa_functions.sql` | [后端/InsForge] | ✅ 已执行 | 将 3 个 Edge Functions 注册到 `functions.definitions` 表 |
| `astron-rpa/frontend/packages/web-app/src/api/` | [前端/astron-rpa] | 调 `robot-service` API | 改为 `@insforge/sdk` `database.from().select()` |
| `astron-rpa/engine/servers/astronverse-scheduler/.../terminal.py` | [前端/astron-rpa] | 调 `POST /api/robot/terminal/register` + `beat` | 改为调用 `POST /functions/rpa-terminal` |
| `astron-rpa/engine/servers/astronverse-scheduler/.../executor.py` | [前端/astron-rpa] | 调 `POST /api/robot/robot-record/save-result` | 改为调用 `POST /functions/rpa-execution` |

### 任务列表

**后端/InsForge 侧（✅ 已完成）：**
- [x] 编写 Edge Functions：rpa-terminal（注册/心跳）、rpa-execution（状态上报）、rpa-crud（CRUD）
- [x] 注册到 `functions.definitions` 表
- [x] 测试函数可用性（全部通过）

**前端/astron-rpa 侧（✅ 已完成）：**
- [x] 前端：将 `api-client.ts` 中 `/api/robot/...` 调用逐一迁移到 InsForge SDK 或 `POST /functions/rpa-*`
- [x] 引擎：将 terminal 注册/心跳改为调用 `POST /functions/rpa-terminal`
- [x] 引擎：将执行结果上报改为调用 `POST /functions/rpa-execution`

**涉及文件**：
- `frontend/packages/shared/src/api-client.ts` — 增加 `invokeRpaFunction` 辅助函数，改造 `records/robots/tasks/market/projects/versions/openapi/mail/component` 模块为 Edge Function 调用
- `frontend/packages/web-app/src/api/resource.ts` — `uploadFile` / `uploadVideoFile` 改为 `rpaApi.resources.upload`
- `engine/servers/astronverse-scheduler/src/astronverse/scheduler/core/terminal/terminal.py` — `register/upload` 改为调用 `rpa-terminal`
- `engine/servers/astronverse-scheduler/src/astronverse/scheduler/core/executor/executor.py` — `get_execute_id` 改为调用 `rpa-execution`

### 验证结果（2026-07-11）

```bash
# rpa-terminal — 注册 ✅
curl -X POST "http://localhost:7130/functions/rpa-terminal?action=register" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"terminalId":"test-term-xxx","name":"Test Terminal","ip":"192.168.1.100","os":"Linux 5.15"}'
# → {"code":200,"data":{"id":"1b581b62-...","status":"registered"},"message":"success"}

# rpa-terminal — 心跳 ✅
curl -X POST "http://localhost:7130/functions/rpa-terminal?action=beat" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"terminalId":"test-term-xxx"}'
# → {"code":200,"data":{"id":"1b581b62-...","status":"alive"},"message":"success"}

# rpa-execution — 状态更新 ✅
curl -X POST "http://localhost:7130/functions/rpa-execution?action=update-status" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"result":"2"}'
# → {"code":200,"data":{"status":"acknowledged","result":"running"},"message":"success"}

# rpa-crud — 机器人列表 ✅
curl -X POST "http://localhost:7130/functions/rpa-crud" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"action":"execute-list","page":1,"pageSize":10}'
# → {"code":200,"data":{"records":[],"total":0},"message":"success"}

# rpa-crud — 机器人详情 ✅
curl -X POST "http://localhost:7130/functions/rpa-crud" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"action":"robot-detail","robotId":"<uuid>"}'
# → {"code":200,"data":{...},"message":"success"}

# rpa-crud — 流程创建 ✅
curl -X POST "http://localhost:7130/functions/rpa-crud" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"action":"process-create","processName":"Test Process"}'
# → {"code":200,"data":{"processId":"bcbae2f8-..."},"message":"success"}
```

---

## 十一、Fix-13: resource-service 文件管理迁移到 InsForge Storage

**优先级**: P2  
**状态**: ✅ 已完成  
**描述**: resource-service 的文件上传/下载/共享功能已通过 InsForge Storage 替代，创建了私有和公开两个 Storage bucket。前端/引擎调用已全部迁移。

### 改造范围

| 侧 | 仓库 | 说明 |
|----|------|------|
| **[后端/InsForge]** | `InsForge/` | 已配置 Storage bucket，测试通过 ✅ |
| **[前端/astron-rpa]** | `astron-rpa/frontend/` | 待修改前端文件上传，从 resource-service 改为 InsForge Storage SDK |
| **[前端/astron-rpa]** | `astron-rpa/engine/` | 待修改引擎企业共享文件上传/下载，改为 InsForge Storage |

### 涉及文件

| 文件 | 仓库 | 当前状态 | 改造目标 |
|------|------|----------|----------|
| `InsForge/`（Storage 配置） | [后端/InsForge] | ✅ 已创建 2 个 bucket | `rpa-files`（私有）+ `rpa-public`（公开） |
| `astron-rpa/frontend/packages/web-app/src/api/resource.ts` | [前端/astron-rpa] | 调 `POST /api/resource/file/upload` | 改为 `storage.from().upload()` |
| `astron-rpa/engine/components/astronverse-enterprise/.../enterprise.py` | [前端/astron-rpa] | 调 `shared-file-upload` + `addSharedFileInfo` | 改为 InsForge Storage |

### 任务列表

**后端/InsForge 侧（✅ 已完成）：**
- [x] 创建 Storage bucket：`rpa-files`（私有）+ `rpa-public`（公开）
- [x] 测试 Storage API 可用性（上传、下载、列出全部通过）

**前端/astron-rpa 侧（✅ 已完成）：**
- [x] 前端：将文件上传迁移到 InsForge Storage SDK `storage.from().upload()`
- [x] 引擎：将企业共享文件上传/下载迁移到 InsForge Storage
- [x] 清理 `backend/resource-service/` 源码引用

**涉及文件**：
- `frontend/packages/shared/src/api-client.ts` — `resources` bucket 改为 `rpa-files` / `rpa-public`
- `frontend/packages/web-app/src/api/resource.ts` — `uploadFile` / `uploadVideoFile` 改为 `rpaApi.resources.upload`
- `engine/components/astronverse-enterprise/src/astronverse/enterprise/enterprise.py` — `upload_to_sharefolder` / `download_from_sharefolder` / `get_shared_variable` 改为 InsForge Storage + DB
- `backend/src/infra/database/migrations/001_create_rpa_tables.sql` — 新增 `shared_files` / `shared_variables` 表
- `docker/docker-compose.yml` — 注释已废弃的 `resource-service` / `robot-service`

### 验证结果（2026-07-11）

```bash
# 创建 Storage bucket ✅
curl -X POST "http://localhost:7130/api/storage/buckets" \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -d '{"bucketName":"rpa-files","isPublic":false}'
# → {"message":"Bucket created successfully","bucketName":"rpa-files"}

# 上传文件到私有 bucket ✅
curl -X PUT "http://localhost:7130/api/storage/buckets/rpa-files/objects/test.txt" \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -F "file=@test.txt"
# → {"bucket":"rpa-files","key":"test.txt","size":25,"mimeType":"text/plain",...}

# 上传文件到公开 bucket ✅
curl -X PUT "http://localhost:7130/api/storage/buckets/rpa-public/objects/test.txt" \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -F "file=@test.txt"
# → {"bucket":"rpa-public","key":"test-public.txt","size":25,...}

# 列出对象 ✅
curl "http://localhost:7130/api/storage/buckets/rpa-files/objects" \
  -H "Authorization: Bearer $ADMIN_TOKEN"
# → {"data":[{...}],"pagination":{"total":1}}

# 公开下载（无需认证）✅
curl "http://localhost:7130/api/storage/buckets/rpa-public/objects/test-public.txt"
# → Hello, InsForge Storage!

# 私有下载（需认证）✅
curl "http://localhost:7130/api/storage/buckets/rpa-files/objects/test.txt" \
  -H "Authorization: Bearer $ADMIN_TOKEN"
# → Hello, InsForge Storage!
```

---

## 十一、Fix-14: 浏览器插件 RPC / trigger 远程 WebSocket

**优先级**: P3  
**状态**: ✅ 已完成  
**描述**: 浏览器插件 RPC 改为直连 browser-bridge HTTP 服务；trigger 远程控制改为 InsForge Realtime（Socket.IO）。

### 改造范围

| 侧 | 仓库 | 说明 |
|----|------|------|
| **[前端/astron-rpa]** | `astron-rpa/engine/components/astronverse-browser/` | 浏览器插件 RPC 改为直连 browser-bridge `/browser/transition` |
| **[前端/astron-rpa]** | `astron-rpa/engine/servers/astronverse-trigger/` | trigger 远程控制改为 InsForge Realtime Socket.IO |

### 涉及文件

| 文件 | 仓库 | 改造内容 |
|------|------|----------|
| `engine/components/astronverse-browser/src/astronverse/browser/browser.py` | [前端/astron-rpa] | `send_browser_rpc` 改为 `POST http://127.0.0.1:19082/browser/transition`，移除对本地网关 `13159` 的依赖 |
| `engine/servers/astronverse-trigger/src/astronverse/trigger/server/server.py` | [前端/astron-rpa] | `WebSocketManager` 改为 `RealtimeManager`，通过 InsForge Realtime 接收 `remote.run` / `remote.stop_current` |
| `engine/servers/astronverse-trigger/src/astronverse/trigger/server/realtime_client.py` | [前端/astron-rpa] | 新增 `TriggerRealtimeClient`，封装 Socket.IO 连接与事件处理 |
| `engine/servers/astronverse-trigger/pyproject.toml` | [前端/astron-rpa] | 新增依赖 `python-socketio[client]>=5.11` |

### 任务列表

- [x] 评估 browser.py RPC 改造方案
- [x] 评估 trigger server.py 改造方案
- [x] 实施改造